name: Build

on:
  push:
    branches:
      - main
      - master
      - "feat/**"
      - "fix/**"
  workflow_dispatch:

# Cancel in-progress runs when a new commit is pushed to the same ref.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CMAKE_BUILD_TYPE: Release
  CMAKE_FLAGS: >-
    -DCMAKE_BUILD_TYPE=Release
    -DCMAKE_CXX_STANDARD=20
    -DCMAKE_CXX_STANDARD_REQUIRED=ON
    -DCMAKE_CXX_EXTENSIONS=OFF
    -DTE_BUILD_COMPILE_CHECK=ON

jobs:
  ubuntu:
    name: ubuntu-latest / Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            ninja-build \
            libasound2-dev libjack-jackd2-dev \
            libcurl4-openssl-dev libfreetype-dev libfontconfig1-dev \
            libx11-dev libxcomposite-dev libxcursor-dev libxext-dev \
            libxinerama-dev libxrandr-dev libxrender-dev \
            libgl1-mesa-dev

      - name: Configure
        run: cmake -S . -B build -G Ninja $CMAKE_FLAGS

      - name: Build
        run: cmake --build build --parallel

  macos:
    name: macos-latest / Release
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install build dependencies
        run: brew install ninja

      - name: Configure
        run: cmake -S . -B build -G Ninja $CMAKE_FLAGS

      - name: Build
        run: cmake --build build --parallel

  # windows-latest-8core runner not available on this fork — disabled.
  # windows:
  #   name: windows-latest-8core / Release
  #   runs-on: windows-latest-8core
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         submodules: recursive
  #
  #     - name: Setup MSVC x64
  #       uses: ilammy/msvc-dev-cmd@v1
  #       with:
  #         arch: x64
  #
  #     - name: Configure
  #       shell: pwsh
  #       run: |
  #         cmake -S . -B build -G Ninja `
  #           -DCMAKE_BUILD_TYPE=Release `
  #           -DCMAKE_CXX_STANDARD=20 `
  #           -DCMAKE_CXX_STANDARD_REQUIRED=ON `
  #           -DCMAKE_CXX_EXTENSIONS=OFF `
  #           -DTE_BUILD_COMPILE_CHECK=ON
  #
  #     - name: Build
  #       run: cmake --build build --parallel
